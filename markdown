# and multiple # to define title

![name](source) to add a figure

**content** to make the content style bold

*content* for italique

[name](href) to make a link style

<href> to make a link but you must use the format http(s)://  or  a email account format to make this work

*, - to make a ul

no.  to make a ol

```python
code   to make a code block style also use a tab or at least four spaces to start a block the code type right after the first symbol indicates the language is 'python'
```

`code` to make a code style inline

--------------------
Make [a link][arbitrary_id] `[a link][arbitrary_id]` then on it's own line anywhere else in the file:  
`[arbitrary_id]: http://macdown.uranusjr.com "Title"`
  
If the link text itself would make a good id, you can link [like this][] `[like this][]`, then on it's own line anywhere else in the file:  
`[like this]: http://macdown.uranusjr.com`  


[arbitrary_id]: http://macdown.uranusjr.com "Title"

[like this]: http://macdown.uranusjr.com  
--------------------

image

[image-id]: path/or/url/to.jpg "Optional Title"
![Alt Image Text](path/to.jpg "Optional Title")
--------------------
Block quote

> to block quote more > to make sub quotes, just like how I make notes on the A4 paper
--------------------
Horizontal 
use --- or ***
--------------------

This is a table:

First Header  | Second Header
------------- | -------------
Content Cell  | Content Cell
Content Cell  | Content Cell

You can align cell contents with syntax like this:

| Left Aligned  | Center Aligned  | Right Aligned |
|:------------- |:---------------:| -------------:|
| col 3 is      | some wordy text |         $1600 |
| col 2 is      | centered        |           $12 |
| zebra stripes | are neat        |            $1 |

The left- and right-most pipes (`|`) are only aesthetic, and can be omitted. The spaces don’t matter, either. Alignment depends solely on `:` marks.

--------------------
Math Syntax
\\[
    A^T_S = B
\\]

or  \\( \\) to make inline equation
